[build]
builder = "NIXPACKS"
buildCommand = "npm run build --workspace apps/bot"

[deploy]
startCommand = "npm run migrate:deploy --workspace apps/bot && npm start --workspace apps/bot"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
